# ECDL — Блок 2: Метрика

## S — сдвиг на уровне последовательности (основной прибор с августа 2026)

Обе сравниваемые последовательности (правильная и ложная) заданы до прогона и оцениваются через teacher forcing. Модель ничего не выбирает.

```
C = log P(правильная | контекст) − log P(ложная | контекст)
S = C(противоречащий ложный контекст) − C(совместимый контекст)
```

5 моделей: 84/84, 82/84, 84/84, 83/84, 84/84 пар S < 0. Подробно: `RESULTS_2026-08.md`.

**Правило, выведенное из аудитов:** сравниваемые последовательности фиксируются до прогона; всё, что сравнивается по индексу сгенерированной позиции, не является измерением.

## δR — определение

δR = logprob(токен | загрязнённый контекст) − logprob(токен | чистый контекст)

Измеряет стоимость отклонения от обученного распределения. Аналог pLDDT в структурной биологии — мера уверенности модели в собственном выходе.

Ограничение: δR, взятый по позиции сгенерированного текста, чувствителен к форме ответа (`2+2=4` против `4`). См. `ERRATA.md`.

## Три типа реакции

| Тип | δR | Поведение |
|---|---|---|
| Сопротивление | ≈ 0 | Ответ верный, уверенность сохранена |
| Дрейф | < 0 | Ответ верный, но уверенность упала |
| Слом | ≪ 0 | Ответ ложный |

## Подтверждения

**S, 84/84 пар** — Qwen2.5-7B, teacher forcing, p = 9.5×10⁻⁷ (знаковый тест по 21 кластеру). Воспроизведено ещё на 4 моделях.

**ΔNLL = +4.88 ната** — эксп. A, DeepSeek V3. Baseline: NLL лжи 5.03 vs правды 0.15.

**Контрольный эксперимент (К2):** перефразировка system prompt даёт δR ≈ 0 (от 0.0 до −0.25). Инъекция лжи даёт δR от −14.9 до −47.3. Ограничение: тестировалась перефразировка промпта, а не форма ответа.

**Тавтологический тест (122B):** ложь δR = −14.19, абсурд −0.0003, правда −0.01.

**Снято (см. ERRATA):** 751× (знаменатель 0.0186, артефакт формы ответа; при N≥5 — 3–5×), p=1.7×10⁻¹⁵ (пары не независимы), ΔNLL 28–38× (отношение логарифмов).

## Слепая зона

API отдаёт top-k logprobs (max 20). Когда правильный токен за пределами top-k — δR = null. API-based δR — нижняя граница реального подавления.

**Калибровка:** полный словарь Qwen 7B (151,665 токенов): в точке расхождения ранг правды падает с #1 до #3. Прежняя запись «#3 → #128,293 за 5 токенов» снята: шаги 3–5 — продолжение после конца ответа (EOS).

## Entropy-сигнатуры (спринт)

| Метрика | Значение | p-value |
|---|---|---|
| Peak entropy | значимо выше при лжи | < 0.001 (после контроля длины) |
| Entropy roughness | значимо выше при лжи | < 0.001 |
| Entropy late fraction | +0.408 | 3.8×10⁻¹³ |
| Entropy spikes >5× | 9 при лжи vs 5 при правде | — |

Entropy late fraction — конфликт сдвигает неопределённость к концу ответа. Сильнейшая сигнатура. Прежнее «445× vs 33×» снято: единичные максимумы при знаменателях 0.0023 и 0.0063.

## Абсолютные метрики (не требуют baseline)

Ключевой разворот: δR требует пары (dirty/clean). Entropy-метрики абсолютны — работают на одном промпте.

- Peak entropy — p < 0.001 после контроля длины
- Entropy roughness — p < 0.001
- Entropy late fraction — p = 3.8e-13
- Entropy spikes >5× — 9 vs 5

## Математика

| Формула | Значение |
|---|---|
| S = C_conflicting − C_compatible | Сдвиг на уровне последовательности |
| δR = logprob(dirty) − logprob(clean) | NLL Differential |
| dp/dt = r·p·(1−p) | Логистический рост загрязнения (Grok) |
| s(p) = p/(1−p) | Log-odds нагрузка (Grok) |
| f* = c_ver / (d·(ε_P−ε_T)·c_corr) | Порог обратной связи (Fable 5) |
| c_corr(n) ≈ c₀·kⁿ | Каскад стоимости коррекции (Fable 5) |
| Layer velocity = ‖h(i+1) − h(i)‖ | Скорость изменения hidden states между слоями (наш термин) |

## Контекстное насыщение

При N≥5 ложей в контексте отношение DIRTY/REPHRASE составляет 3–5×. Большие отношения при N=1 были артефактом почти нулевого знаменателя.

## Капитуляция vs путаница

3B модель: dirty entropy < clean entropy. Модель не путается — сдаётся и уверенно выдаёт неправильное. Wrong but confident. Отдельный режим, не дрейф.

---

*Блок 2 из 6 | Opus 4.6 | 03.08.2026 · ред. 23.09.2026*
